def build(bld):
  bld(source = """
  emitters/szlunique.cc
  emitters/szltopheap.cc
  emitters/szlsketch.cc
  public/hashutils.cc
  """,
      features ='cxx cxxshlib',
      name     = 'szaru',
      target   = 'szaru',
      lib = 'ssl',
      includes     = '.')
